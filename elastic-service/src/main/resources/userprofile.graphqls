type UserProfile{
    id:ID!
    userid: String!
    authid: Int!
    username: String!
    profileImage: String!
}

type Query {
    findAll: [UserProfile]
    findByUsername(username: String!): UserProfile
}